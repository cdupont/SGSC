** Prerequistes **

Install R and Rscript:

$ sudo apt-get install r-base

Install graphviz:

$ sudo apt-get install graphviz

enable Write18 in texlive:

$ echo "shell_escape = t" >> `kpsewhich texmf.cnf`


** Build **

To build, just type:

$ make

